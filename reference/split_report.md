# Split a fixation or message report into hierarchical dataframes

Split a fixation or message report into hierarchical dataframes

## Usage

``` r
split_report(report, drop_empty_columns = TRUE)
```

## Arguments

- drop_empty_columns:

  Logical. If TRUE, columns that are all NA will be dropped.
